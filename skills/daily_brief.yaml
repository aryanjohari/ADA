id: daily_brief
description: Enqueue a daemon goal with SQL-grounded daily brief text (flags + snapshot).
risk_tier: low
ring: 2
motor_type: goal_add
allowed_params:
  - goal_text
required_params: []
mission_required: true
require_approval: false
