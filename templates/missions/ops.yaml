name: jarvis-ops
slug: jarvis-ops
title: Jarvis ops
pack: core-ops
brief_md: |
  Operator intent: nervous-system ops for this profile — daily brief and mission tick only.
  No publish pipelines unless you switch to an ISR template.
defaults_json:
  project_id: jarvis-main
  campaign_id: ops
skills_enabled:
  - daily_brief
  - mission_tick_dry_run
schedule_hint_json:
  version: 1
  jobs:
    - id: daily_brief
      min_interval_hours: 24
      action:
        type: enqueue_goal
        goal_text: "Daily ops brief — run daily_brief skill or ada brief --enqueue"
recommended_cron:
  - comment: Daily deterministic brief (operator installs manually)
    line: "0 7 * * * ada brief --mission jarvis-ops"
  - comment: Mission tick (weekly example — adjust cadence)
    line: "0 6 * * 0 ada mission tick --mission jarvis-ops"
knowledge_sources: []
risk_summary: Ops programme for jarvis profile — review cron snippets before apply.
