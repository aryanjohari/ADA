name: isr-publish
slug: isr-nz-example
title: ISR NZ publish
pack: isr-publish
brief_md: |
  Operator intent: NZ ISR entity pages — enrich graph, gate on unique facts, draft and deploy.
  Set project_id, campaign_id, niche, and entity_id in defaults_json before apply.
defaults_json:
  project_id: jarvis-main
  campaign_id: isr-nz
  niche: isr
  delivery_scaffold: v1
skills_enabled:
  - publish_entity_v1
  - publish_keyword_v1
schedule_hint_json:
  version: 1
  jobs: []
knowledge_sources: []
recommended_cron:
  - comment: Mission tick (operator adjusts cadence)
    line: "0 8 * * * ada mission tick --mission isr-nz-example"
risk_summary: ISR publish pack — requires approval for deploy; review entity and cron before apply.
