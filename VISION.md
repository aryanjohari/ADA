# ADA — border vision (draft)
Personal embodied agent whose body is this Raspberry Pi.
Blank-slate redesign on branch rewrite/v1-body.
Portfolio/factory code remains on main.
Constraints for brainstorm (soft):
- Prefer evidence from papers + this machine's real hardware
- LLM is cortex/orchestrator, not the whole organism
- No custom Linux distro; Pi OS Lite host is fine
- main branch must never be force-rewritten
